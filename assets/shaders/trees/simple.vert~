#version 450

in vec2 uv;
in vec3 position;

uniform mat4 view;
uniform mat4 transform;
uniform mat4 projection;

out vec2 uv_coords;

void main() {
  uv_coords = uv;
  gl_Position = projection * transform * vec4(position, 1.0);
}
